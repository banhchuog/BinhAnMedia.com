{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build"
  },
  "deploy": {
    "startCommand": "DATABASE_URL=${DATABASE_URL:-$DATABASE_PUBLIC_URL} npm run db:migrate && DATABASE_URL=${DATABASE_URL:-$DATABASE_PUBLIC_URL} npm run db:seed && npm run start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
